//creamos un componente de botón
// expotamos la función para que pueda ser utilizada en otras partes de la aplicación
export function MyButton() {
    return (
        <button> soy un botón</button>
    );
}

